# Fix Python 3.13 audioop removal (breaks pydub which gradio imports)
audioop-lts; python_version >= "3.13"

# Minimal build-time deps — ONLY what Gradio UI needs to start
matplotlib>=3.7.0
numpy>=1.24.0

# DO NOT pin huggingface_hub here — gradio 4.44.0 needs a specific
# compatible version and will install it automatically.

# ALL ML deps (torch, trl, unsloth, etc.) are installed at RUNTIME
# inside train_gpu.py when the T4 GPU is live
