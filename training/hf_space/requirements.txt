# ─── CascadeDebug GRPO Training — HF Space Dependencies ──────────────────────
# All CUDA packages from the SAME PyTorch index to prevent CUDA version mismatch.
# The Gradio SDK Space on L4 has NVIDIA drivers pre-installed.

--extra-index-url https://download.pytorch.org/whl/cu124

# Fix Python 3.13 audioop removal (breaks pydub which gradio imports)
audioop-lts; python_version >= "3.13"

# Core ML — all from cu124 index for consistent CUDA versions
torch>=2.5.0
torchvision>=0.20.0
torchaudio>=2.5.0

# Unsloth for 4-bit quantization
unsloth

# Training
trl>=0.12.0
datasets
accelerate>=0.30.0
peft>=0.10.0
bitsandbytes>=0.43.0
scipy

# Plotting
matplotlib>=3.7.0
numpy>=1.24.0

# Hub push
huggingface_hub
