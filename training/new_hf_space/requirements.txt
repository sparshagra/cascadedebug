# ── Hugging Face Space (Docker SDK) ─────────────────────────────────────────
# Dependencies are installed in the Dockerfile (Python 3.11 + CUDA 12.1).
# This file documents the intended pins for local debugging only.
#
# Docker install (authoritative):
#   torch==2.5.1+cu121  (from PyTorch cu121 index)
#   unsloth[cu121-torch251]==2025.11.4
#
# Local CPU-only installs will not run GRPO; use the Docker image or HF Space.

unsloth[cu121-torch251]==2025.11.4
torch==2.5.1+cu121
torchvision==0.20.1+cu121
torchaudio==2.5.1+cu121
gradio>=5.25.0
matplotlib>=3.7.0
scipy
huggingface_hub
